kind=defined
names=to_f
visibility=public

--- to_f -> Float

自身を [[c:Float]] に変換します。

@raise RangeError 虚部が 0 ではない場合に発生します。

例:

  Complex(3).to_f    # => 3.0
  Complex(3.5).to_f  # => 3.5
  Complex(3, 2).to_f # => RangeError

