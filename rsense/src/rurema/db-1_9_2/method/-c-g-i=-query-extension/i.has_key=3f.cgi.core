kind=defined
names=has_key?,include?,key?
visibility=public

--- has_key?(*args) -> bool
--- key?(*args) -> bool
--- include?(*args) -> bool

与えられたキーがクエリに含まれている場合は、真を返します。
そうでない場合は、偽を返します。

@param args キーを一つ以上指定します。

