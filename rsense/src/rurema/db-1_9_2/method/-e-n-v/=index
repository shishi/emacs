.[]	ENV.[]
.[]=	ENV.[]=
.assoc	ENV.assoc
.clear	ENV.clear
.delete	ENV.delete
.delete_if	ENV.delete_if
.each	ENV.each
.each_key	ENV.each_key
.each_pair	ENV.each
.each_value	ENV.each_value
.empty?	ENV.empty?
.fetch	ENV.fetch
.has_key?	ENV.has_key?
.has_value?	ENV.has_value?
.include?	ENV.has_key?
.index	ENV.index
.inspect	ENV.inspect
.invert	ENV.invert
.keep_if	ENV.keep_if
.key	ENV.index
.key?	ENV.has_key?
.keys	ENV.keys
.length	ENV.length
.member?	ENV.has_key?
.rassoc	ENV.rassoc
.rehash	ENV.rehash
.reject	ENV.reject
.reject!	ENV.delete_if
.replace	ENV.replace
.select	ENV.select
.select!	ENV.keep_if
.shift	ENV.shift
.size	ENV.length
.store	ENV.[]=
.to_a	ENV.to_a
.to_hash	ENV.to_hash
.to_s	ENV.to_s
.update	ENV.update
.value?	ENV.has_value?
.values	ENV.values
.values_at	ENV.values_at
