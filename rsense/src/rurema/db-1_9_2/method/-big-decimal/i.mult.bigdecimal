kind=defined
names=mult
visibility=public

--- mult(b, n)

以下のように使用します。

    c = a.mult(b,n)

c = a * b を最大で n 桁まで計算します。
a * b の精度が n より大きいときは
[[m:BigDecimal.mode]] で指定された方法で丸められます。
n がゼロなら * と同じです。 

