name=st_delete
private=false
macro=false
type=int
filename=st.c
params=(register st_table *table, register char **key, char **value)


*key に対応する値をテーブルから削除し、*key、*value に登録時のキーと
値を書きこむ。返り値は削除したかどうか。

